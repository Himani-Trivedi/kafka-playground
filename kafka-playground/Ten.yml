info:
  name: Ten
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8081/api/messages/ten
  body:
    type: json
    data: |-
      {
        "message" : "Himani"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
