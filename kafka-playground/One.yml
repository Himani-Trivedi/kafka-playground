info:
  name: One
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8083/api/messages/one
  body:
    type: json
    data: |-
      {
        "message" : "Himani"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
