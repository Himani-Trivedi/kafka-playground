info:
  name: Five
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8083/api/messages/five
  body:
    type: json
    data: |-
      {
        "message" : "sent from docker image"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
