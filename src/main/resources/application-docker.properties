# Docker-specific configuration
# This file will be automatically used when spring.profiles.active=docker is set

# Override any properties that need Docker-specific values
# Environment variables will still take precedence due to ${VAR:default} syntax in main application.properties
